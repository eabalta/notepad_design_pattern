package darkMode;

import javax.swing.*;
import java.awt.*;

// Product olan JPanel elemanindan ConcreteProduct olan darkModePanel elemani extend ediliyor.

public class darkModePanel extends JPanel {
    public darkModePanel() {
        setBackground(Color.DARK_GRAY);
    }
}
